{
  "name": "whats-on-tap",
  "description": "",
  "author": "Stahlneckr",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node index.js",
    "precommit": "lint-staged",
    "update-packages": "sudo npm install -g npm-check-updates && npm-check-updates -u"
  },
  "lint-staged": {
    "*.{js,md}": [
      "prettier --write --trailing-comma es5 --arrow-parens always --print-width 160"
    ],
    "*.js": [
      "eslint --ignore-pattern node_modules/ _*"
    ]
  },
  "dependencies": {
    "actions-on-google": "^1.7.0",
    "puppeteer": "^1.0.0",
    "winston": "^2.4.0"
  },
  "devDependencies": {
    "eslint": "^4.15.0",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-config-prettier": "^2.9.0",
    "eslint-plugin-import": "^2.8.0",
    "husky": "^0.14.3",
    "lint-staged": "^6.0.0",
    "prettier": "^1.9.2"
  }
}
